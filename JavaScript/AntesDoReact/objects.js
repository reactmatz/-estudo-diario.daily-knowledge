// objetos em javascript

const user = {
  name:'matheus',
  idade:23,
  adress:{
    street:'rua de tal',
    number:253
  }
}
// const validation = ('name' in user) ----- 
// console.log(Object.keys(user));
// console.log(Object.values(user))
// console.log(Object.entries(user))  --- metodo que retorna o vetor [] com cada valor separados por virgula organizado como se fosse um objeto

// ---- operador 'in' serve para validar se existe um objeto dentro outro
// ---- Object.keys() e um metodo que retorna as chaves em vetor [] do objeto selecionado, o mesmo serve para Object.values() para retornar os valores


// ------------------- //


