class Pessoa {
    constructor(nome, idade) {
      this.nome = nome;
      this.idade = idade;
    }
  }
  
  let pessoa2 = new Pessoa("Bob", 35);
  

{
    nome: '' // voce vai receber o argumento, que e uma variavel, que vai receber um valor quando a funcao for chamada,
}